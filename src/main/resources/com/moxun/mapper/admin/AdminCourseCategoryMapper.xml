<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.moxun.mapper.admin.AdminCourseCategoryMapper">

    <insert id="addCategory">
        INSERT INTO course_categories (name, icon, parent_id, sort, create_time, update_time)
        VALUES (#{name}, #{icon}, #{parentId}, #{sort}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateCategory">
        update course_categories
            <set>
                <if test="name != null and name != ''">name = #{name},</if>
                <if test="icon != null">icon = #{icon},</if>
                <if test="parentId != null">parent_id = #{parentId},</if>
                <if test="sort != null">sort = #{sort},</if>
                <if test="updateTime != null">update_time = #{updateTime}</if>
            </set>
        where id = #{id}
    </update>

    <delete id="delParentCourseCategory">
        delete from course_categories
            where id = #{parentId} and parent_id = #{parentId}
    </delete>
    <delete id="delPosterityCategory">
        delete from course_categories
        where parent_id in 
        <foreach collection="list" item="item" separator="," close=")" open="(" index="index">
            #{item}
        </foreach>
    </delete>
</mapper>