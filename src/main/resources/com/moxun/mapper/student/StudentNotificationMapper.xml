<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.moxun.mapper.student.StudentNotificationMapper">

    <!-- 查询我的通知列表 -->
    <select id="listMyNotifications" resultType="com.moxun.Pojo.Entity.Notification">
        SELECT
            id,
            user_id,
            type,
            title,
            content,
            is_read,
            link,
            sender_id,
            create_time
        FROM notifications
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 统计未读通知数量 -->
    <select id="countUnread" resultType="long">
        SELECT COUNT(1)
        FROM notifications
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <!-- 标记单条通知已读 -->
    <update id="markAsRead">
        UPDATE notifications
        SET is_read = 1
        WHERE id = #{notificationId} AND user_id = #{userId}
    </update>

    <!-- 全部标记已读 -->
    <update id="markAllAsRead">
        UPDATE notifications
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>
